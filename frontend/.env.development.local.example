# QA probe enabled for dev/preview E2E
VITE_QA_PROBE=1

# Deterministic default symbol (tests assume this)
VITE_DEFAULT_SYMBOL=AAPL

# Local API endpoints (safe fallbacks if backend is unavailable)
VITE_API_URL=http://localhost:8001
VITE_WS_URL=ws://localhost:8001/ws
